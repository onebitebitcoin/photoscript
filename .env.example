# PhotoScript - Environment Variables Template
# .env.example 파일을 .env로 복사하고 필요시 값을 수정하세요
#
# 12-Factor App 원칙에 따라 모든 설정은 환경 변수로 관리됩니다.
# 참고: https://12factor.net/config

# ====================
# Backend Configuration
# ====================

# [필수] Database
# 개발: SQLite 또는 PostgreSQL
# 프로덕션: PostgreSQL 필수 (SQLite 사용 불가)
#
# SQLite (개발용):
DATABASE_URL=sqlite:///./photoscript.db
#
# PostgreSQL (권장 - Dev/Prod Parity):
# docker-compose -f docker-compose.dev.yml up -d 실행 후 사용
# DATABASE_URL=postgresql://photoscript:photoscript@localhost:5432/photoscript

# [필수] 보안 키
# 프로덕션에서는 반드시 강력한 랜덤 문자열로 변경해야 합니다.
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_secret_key_here_change_in_production

# [선택] 로그 레벨
# DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# [선택] 환경 (development | production | test)
# 프로덕션 환경에서는 SECRET_KEY와 PostgreSQL이 필수입니다.
ENVIRONMENT=development

# [선택] Gunicorn 워커 수 (프로덕션)
# 권장: CPU 코어 수 * 2 + 1
# WORKERS=2

# [선택] External APIs
PEXELS_API_KEY=your_pexels_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# ====================
# Testing Configuration
# ====================

# [선택] 테스트용 데이터베이스 URL
# PostgreSQL로 테스트하려면 설정하세요 (Factor X: Dev/Prod Parity)
# TEST_DATABASE_URL=postgresql://photoscript:photoscript@localhost:5432/photoscript_test

# ====================
# Frontend Configuration
# ====================

# API URL
VITE_API_URL=http://localhost:7100/api/v1

# Application
VITE_APP_NAME=PhotoScript
VITE_APP_VERSION=1.0.0

# ====================
# Railway (Production)
# ====================

# Railway가 자동으로 설정하는 변수들:
# - PORT: 서버 포트
# - DATABASE_URL: PostgreSQL 연결 URL (Railway PostgreSQL 사용 시)
#
# Railway 배포 시 필수 설정:
# - SECRET_KEY: 강력한 랜덤 문자열 (Railway 대시보드에서 설정)
# - ENVIRONMENT: production
